# Used only for local development with `netlify dev`.
# In production, set these in Netlify → Site configuration → Environment variables.

# AI Career Coach (optional) — get a free key at https://console.groq.com/keys
GROQ_API_KEY=your_groq_key_here

# Job search (optional) — get free keys at https://developer.adzuna.com/
ADZUNA_APP_ID=your_adzuna_app_id
ADZUNA_APP_KEY=your_adzuna_app_key
